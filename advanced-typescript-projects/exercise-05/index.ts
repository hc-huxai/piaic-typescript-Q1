let author: string = "Wayne Dyer";
let quote: string =
  "If you change the way you look at things, the things you look at... change.";
let message: string = `${author} once said, "${quote}".`;

console.log(message);
